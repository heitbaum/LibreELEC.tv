From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Rudi Heitbaum <rudi@heitbaum.com>
Date: Mon, 3 Aug 2026 00:05:59 +1000
Subject: [PATCH] arm64: dts: imx8mq-phanbell: describe the QCA6174 Bluetooth

The Coral Dev Board's QCA6174 has its Bluetooth side on uart2 with flow
control, and a power line on gpio3 line 6. Describe it with the serdev
binding so hci_qca can drive it.

The vendor devicetree does not use serdev - it has a downstream
"fsl,mxc_bt_rfkill" shim and leaves the rest to userspace hciattach -
but it does give the wiring. Its bt-power-gpios names gpio3 line 6, and
its uart2 pinctrl group mixes the four uart pins in with that same GPIO
as NAND_DATA00_GPIO3_IO6. Note the flow control pins are muxed from the
UART4 pads, not the UART2 ones.

Two details are not derivable from the vendor tree and are set by
convention here:

  - enable-gpios polarity. The vendor marks its bt-power-gpios active
    low, but that is a different property on a different binding. QCA
    BT_EN is active high in the upstream users, so use that; the failure
    mode if it is wrong is simply no hci0.

  - clocks. The binding requires a 32.768kHz input and the vendor node
    describes none, so it is hardwired on this board. The BD71837 is a
    32768Hz provider and is the plausible source.

Signed-off-by: Rudi Heitbaum <rudi@heitbaum.com>
---
 .../boot/dts/freescale/imx8mq-phanbell.dts    | 30 +++++++++++++++++++
 1 file changed, 30 insertions(+)

diff --git a/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts b/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
index 33ca224..9505357 100644
--- a/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
+++ b/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
@@ -501,6 +501,21 @@
 	status = "okay";
 };
 
+&uart2 {
+	pinctrl-names = "default";
+	pinctrl-0 = <&pinctrl_uart2>;
+	uart-has-rtscts;
+	status = "okay";
+
+	bluetooth {
+		compatible = "qcom,qca6174-bt";
+		pinctrl-names = "default";
+		pinctrl-0 = <&pinctrl_bt>;
+		enable-gpios = <&gpio3 6 GPIO_ACTIVE_HIGH>;
+		clocks = <&pmic>;
+	};
+};
+
 &usdhc1 {
 	pinctrl-names = "default", "state_100mhz", "state_200mhz";
 	pinctrl-0 = <&pinctrl_usdhc1>;
@@ -649,6 +664,21 @@
 		>;
 	};
 
+	pinctrl_bt: btgrp {
+		fsl,pins = <
+			MX8MQ_IOMUXC_NAND_DATA00_GPIO3_IO6	0x19
+		>;
+	};
+
+	pinctrl_uart2: uart2grp {
+		fsl,pins = <
+			MX8MQ_IOMUXC_UART2_RXD_UART2_DCE_RX	0x49
+			MX8MQ_IOMUXC_UART2_TXD_UART2_DCE_TX	0x49
+			MX8MQ_IOMUXC_UART4_RXD_UART2_DCE_CTS_B	0x49
+			MX8MQ_IOMUXC_UART4_TXD_UART2_DCE_RTS_B	0x49
+		>;
+	};
+
 	pinctrl_uart1: uart1grp {
 		fsl,pins = <
 			MX8MQ_IOMUXC_UART1_RXD_UART1_DCE_RX		0x49
