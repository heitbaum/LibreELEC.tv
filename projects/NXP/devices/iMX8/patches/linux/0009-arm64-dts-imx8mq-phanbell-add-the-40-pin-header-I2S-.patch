From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Rudi Heitbaum <rudi@heitbaum.com>
Date: Mon, 3 Aug 2026 22:30:00 +1000
Subject: [PATCH] arm64: dts: imx8mq-phanbell: add the 40-pin header I2S card

sai1 goes to the 40-pin expansion header as plain I2S with no codec on
the far side, which the vendor kernel exposes as its "40-pin Header"
card. linux,spdif-dit stands in for the far end.

32 bit slots regardless of the sample width: sai1 divides its 24.576 MHz
root by an even integer only, so a 24 bit frame at 48 kHz would ask for a
bit clock of 2.304 MHz, a ratio of 10.67, and hw_params would fail with
-EINVAL. 32 bit slots ask for 3.072 MHz, a ratio of 8.

spdif-dit is a transmitter, so this card is playback only where the
vendor's dummy gave both directions.
---
 .../boot/dts/freescale/imx8mq-phanbell.dts    | 46 +++++++++++++++++++
 1 file changed, 46 insertions(+)

diff --git a/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts b/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
index e80c16783d1a..1e3d818c2a69 100644
--- a/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
+++ b/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
@@ -21,6 +21,32 @@ memory@40000000 {
 		reg = <0x00000000 0x40000000 0 0x40000000>;
 	};
 
+	header_codec: spdif-transmitter {
+		compatible = "linux,spdif-dit";
+		#sound-dai-cells = <0>;
+	};
+
+	sound-header {
+		compatible = "simple-audio-card";
+		simple-audio-card,name = "40-pin Header";
+		simple-audio-card,format = "i2s";
+
+		/*
+		 * 32 bit slots regardless of the sample width. sai1 divides
+		 * its 24.576 MHz root by an even integer only, so a 24 bit
+		 * frame at 48 kHz would ask for a ratio of 10.67 and fail.
+		 */
+		simple-audio-card,cpu {
+			sound-dai = <&sai1>;
+			dai-tdm-slot-num = <2>;
+			dai-tdm-slot-width = <32>;
+		};
+
+		simple-audio-card,codec {
+			sound-dai = <&header_codec>;
+		};
+	};
+
 	busfreq {
 		status = "disabled";
 	};
@@ -278,6 +304,16 @@ &i2c3 {
 	status = "okay";
 };
 
+&sai1 {
+	pinctrl-names = "default";
+	pinctrl-0 = <&pinctrl_sai1>;
+	assigned-clocks = <&clk IMX8MQ_CLK_SAI1>;
+	assigned-clock-parents = <&clk IMX8MQ_AUDIO_PLL1_OUT>;
+	assigned-clock-rates = <24576000>;
+	fsl,sai-mclk-direction-output;
+	status = "okay";
+};
+
 &ecspi1 {
 	pinctrl-names = "default";
 	pinctrl-0 = <&pinctrl_ecspi1 &pinctrl_ecspi1_cs>;
@@ -566,4 +602,14 @@ pinctrl_wdog: wdoggrp {
 			MX8MQ_IOMUXC_GPIO1_IO02_WDOG1_WDOG_B 0xc6
 		>;
 	};
+
+	pinctrl_sai1: sai1grp {
+		fsl,pins = <
+			MX8MQ_IOMUXC_SAI1_MCLK_SAI1_MCLK	0xd6
+			MX8MQ_IOMUXC_SAI1_TXFS_SAI1_TX_SYNC	0xd6
+			MX8MQ_IOMUXC_SAI1_TXC_SAI1_TX_BCLK	0xd6
+			MX8MQ_IOMUXC_SAI1_TXD0_SAI1_TX_DATA0	0xd6
+			MX8MQ_IOMUXC_SAI1_RXD0_SAI1_RX_DATA0	0xd6
+		>;
+	};
 };
-- 
2.47.1

