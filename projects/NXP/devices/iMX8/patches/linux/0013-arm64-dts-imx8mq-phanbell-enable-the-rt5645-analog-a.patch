From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Rudi Heitbaum <rudi@heitbaum.com>
Date: Mon, 3 Aug 2026 00:41:52 +1000
Subject: [PATCH] arm64: dts: imx8mq-phanbell: enable the rt5645 analog audio

The Coral Dev Board has a Realtek RT5645 codec on i2c3 at 0x1a driving
the 3.5mm jack, fed from SAI2. Describe it with simple-audio-card - the
codec driver is upstream, only the machine glue was missing.

Headphone detect and the codec interrupt are both on gpio5 line 4; with
realtek,jd-mode = <0> the driver reads the line through hp-detect-gpios
and uses the i2c interrupt as the edge trigger.

The widgets and routing follow the vendor kernel's DAPM graph on this
board: the jack fed from HPOL and HPOR, the headset mic tied to
micbias1, and the two onboard microphones on DMIC L1 and R1. There is
deliberately no speaker widget, because the vendor card has none either.

Note micbias1 is a supply widget, so it has to be the source rather than
the sink: snd_soc_dapm_add_path() rejects a non-supply source feeding a
supply sink. The vendor's 4.14 graph has that pair the other way round.

Only the SAI2 transmit clocks are muxed. fsl_sai defaults to RCR2.SYNC
set, so the receive side takes its bit and frame clock from the transmit
side, which is what a codec with a single BCLK/LRCK pair needs.

Signed-off-by: Rudi Heitbaum <rudi@heitbaum.com>
---
 .../boot/dts/freescale/imx8mq-phanbell.dts    | 58 +++++++++++++++++++
 1 file changed, 58 insertions(+)

diff --git a/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts b/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
index ea44ab7..2b8d18d 100644
--- a/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
+++ b/arch/arm64/boot/dts/freescale/imx8mq-phanbell.dts
@@ -21,6 +21,33 @@
 		reg = <0x00000000 0x40000000 0 0x40000000>;
 	};
 
+	sound-analog {
+		compatible = "simple-audio-card";
+		simple-audio-card,name = "Coral Analog";
+		simple-audio-card,format = "i2s";
+		simple-audio-card,bitclock-master = <&sound_cpu>;
+		simple-audio-card,frame-master = <&sound_cpu>;
+		simple-audio-card,mclk-fs = <256>;
+
+		simple-audio-card,widgets = "Headphone", "Headphone Jack",
+					    "Microphone", "Headphone Mic",
+					    "Microphone", "Internal Mic";
+
+		simple-audio-card,routing = "Headphone Jack", "HPOL",
+					    "Headphone Jack", "HPOR",
+					    "Headphone Mic", "micbias1",
+					    "DMIC L1", "Internal Mic",
+					    "DMIC R1", "Internal Mic";
+
+		sound_cpu: simple-audio-card,cpu {
+			sound-dai = <&sai2>;
+		};
+
+		simple-audio-card,codec {
+			sound-dai = <&rt5645>;
+		};
+	};
+
 	busfreq {
 		status = "disabled";
 	};
@@ -292,6 +319,27 @@
 	pinctrl-names = "default";
 	pinctrl-0 = <&pinctrl_i2c3>;
 	status = "okay";
+
+	rt5645: audio-codec@1a {
+		compatible = "realtek,rt5645";
+		reg = <0x1a>;
+		#sound-dai-cells = <0>;
+		interrupt-parent = <&gpio5>;
+		interrupts = <4 IRQ_TYPE_EDGE_BOTH>;
+		hp-detect-gpios = <&gpio5 4 GPIO_ACTIVE_HIGH>;
+		realtek,dmic1-data-pin = <2>;
+		realtek,jd-mode = <0>;
+	};
+};
+
+&sai2 {
+	pinctrl-names = "default";
+	pinctrl-0 = <&pinctrl_sai2>;
+	assigned-clocks = <&clk IMX8MQ_CLK_SAI2>;
+	assigned-clock-parents = <&clk IMX8MQ_AUDIO_PLL1_OUT>;
+	assigned-clock-rates = <24576000>;
+	fsl,sai-mclk-direction-output;
+	status = "okay";
 };
 
 &pcie0 {
@@ -637,4 +685,14 @@
 			MX8MQ_IOMUXC_NAND_DATA05_GPIO3_IO11		0x16
 		>;
 	};
+
+	pinctrl_sai2: sai2grp {
+		fsl,pins = <
+			MX8MQ_IOMUXC_SAI2_MCLK_SAI2_MCLK	0xd6
+			MX8MQ_IOMUXC_SAI2_TXFS_SAI2_TX_SYNC	0xd6
+			MX8MQ_IOMUXC_SAI2_TXC_SAI2_TX_BCLK	0xd6
+			MX8MQ_IOMUXC_SAI2_TXD0_SAI2_TX_DATA0	0xd6
+			MX8MQ_IOMUXC_SAI2_RXD0_SAI2_RX_DATA0	0xd6
+		>;
+	};
 };
